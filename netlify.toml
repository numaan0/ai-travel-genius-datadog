[build]
  # This is the folder where your package.json lives
  base = "ai-travel-genius" 
  
  # This runs inside the 'base' folder
  command = "npm run build" 
  
  # This is where the build output goes, relative to the base folder
  publish = ".next" 

[[plugins]]
  package = "@netlify/plugin-nextjs"